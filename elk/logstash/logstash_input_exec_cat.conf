input {
  exec {
    command => "cat data/event.txt"
    codec => "json"
    interval => 5
  }
}

filter{
  if [message] =~ /^\s*$/ {
  drop { }
  }
    json{
        source => "message"
    }
}

output {
  stdout {
    codec => rubydebug
  }
}